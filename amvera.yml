meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: requirements.txt
  additionalCommands: python scripts/verify_deploy.py

run:
  command: python src/app.py
  containerPort: 80
